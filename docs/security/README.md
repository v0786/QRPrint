# Security Baseline

- Server-side authz is required for role enforcement.
- Never trust browser-provided prices or roles.
- Keep payment secrets server-side only.
- Document binaries must remain in private object storage with short-lived signed URLs.
